{
  "name": "dynamics-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8"
  }
}
